 <p>
    <ul class="list-group list-unstyled">
        <% @articles.each do |article| %>
        <div class="list-group-flush">
         <li class="fs-2"><%= link_to article.title, article_path(article) %></li>
         <li class="list-group-item"><%= article.content%></li>
        </div>
        <% end %>
    </ul>
 </p>
